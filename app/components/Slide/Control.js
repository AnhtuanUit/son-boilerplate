import styled from 'styled-components';

const NextBtn = styled.img`
  position: absolute;
  right: calc(50% - 507px);
  top: 32%;
  width: 24px;
`;

const PreBtn = styled.img`
  position: absolute;
  top: 32%;
  left: calc(50% - 507px);
  width: 24px;
`;

export  { NextBtn, PreBtn };
